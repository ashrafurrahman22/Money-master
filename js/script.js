document.getElementById('calculate-button').addEventListener('click', function(){

            // get income input and convert to numbers
            const incomeInput = document.getElementById('income-input');
            const previousIncomeAmountText = incomeInput.value;
            const previousIncomeAmount = parseInt(previousIncomeAmountText);

            // get food, rent & others input
            // food
            const foodInput = document.getElementById('food-input');
            const foodAmountText = foodInput.value;
            const foodAmount = parseInt(foodAmountText);

            // rent
            
})